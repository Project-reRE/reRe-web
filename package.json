{
  "name": "reRe-web",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "dotenv -- turbo run dev",
    "web": "dotenv -- turbo run dev --filter=web -p 3000 ",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@types/node": "^20.11.24",
    "dotenv": "^16.4.5",
    "dotenv-cli": "^7.4.2",
    "prettier": "^3.2.5",
    "turbo": "^2.0.5",
    "typescript": "^5.4.5"
  },
  "packageManager": "pnpm@9.4.0",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@tanstack/react-query": "^5.49.2"
  }
}
